Installation
============

First, install PyTorch from `http://pytorch.org/ <http://pytorch.org/>`_.

Then, you can install Cogitare from pip, using::

    pip install cogitare

Test the installation by::

    >>> import cogitare
    >>> cogitare.__version__
    '0.1.0'
